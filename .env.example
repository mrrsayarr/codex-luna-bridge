# Optional overrides. This project does not auto-load .env files.
# Export these values in your shell/launcher only when you need to change defaults.
TASK_STATE_PATH=.runtime/tasks.json
MAX_REWORK_ROUNDS=2
LUNA_BROKER_PORT=8788
LUNA_BROKER_PATH=/mcp
